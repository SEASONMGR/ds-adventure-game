# 小游戏素材需求 · 美术侧响应

> 答复程序侧《素材交接-程序侧答复.md》§三、§四，以及《小游戏素材需求.md》的完成情况。

## 一、《小游戏素材需求.md》完成情况

| 需求 | 交付 | 自检 |
|---|---|---|
| §二 音效 15 个（P0 8 / P1 3 / P2 4） | `assets/sounds/se_*.wav` | **格式与时长 15/15 合格** |
| §三 图像 P0 九枚游戏图标 48×48 | `assets/sprites/ui/icon_game_*.png` | **RGBA + 尺寸 9/9** |
| §三 图像 P1 徽记 160×160 ×2 | `shell_badge_win/lose.png` | **2/2** |
| §三 图像 P2 共用外框 1024×600 | `shell_frame.png` | **1/1** |
| §四 复用件核对 | — | **既有件 14/14 + deco 5/5 在位** |
| §五 逐章主题化预研 | 10 张剧情底图 `sprites/ui/minigames/` | 加成，不阻塞验收 |

复验：`python tools/verify_minigame_assets.py`

## 二、答复 §三 的 6 条（我方原说法已过期，按你们的机制更正）

| # | 更正 |
|---|---|
| 1 | 音频走**插件槽** `@plugin(audio) | play | <路径> | se` —— 不再提「新增 sound 节点」 |
| 2 | 标题「退出」用现成的 `@plugin(quit)` —— 不再提「扩展 action」 |
| 3 | **CSS 只有 `.story-btn`**：我方原写的 `primary/normal/small` 作废。**建议就用 `.story-btn`**；
    若日后要三档视觉差异，我可按「主/次/小」产出三套九宫格贴图（需你们确认是否值得） |
| 4 | 横幅用 `char` 节点承载 —— 与我方原建议一致；映射表见 `CG-SE-横幅-触发对照表.md` §二 |
| 5 | `music` 节点已废 → 不再提「换 music 路径」。**BGM 现已到位**（36 首 / 14 类 + `bgm_map.json`），
    接法 `@plugin(audio) | loop | assets/sounds/bgm/bgm_<类>_01.mp3 | bgm` |
| 6 | 半透明立绘走 `ImageView` 天然满足 —— 确认无需特殊处理 |

## 三、答复 §四 的四件拍板

### 4.1 横幅映射 ✅ 已交
`CG-SE-横幅-触发对照表.md` §二 给出「剧本 `st:` 原文 → 横幅文件」查表；
**实测源剧本 23 处 `st:`**，与你们更正后的统计一致。

### 4.2 CG / SE 触发时机 ✅ 已交（走 (b) 方案）
同文档 §四（CG 21 条挂载点）与 §五（SE 分全局 UI 音 / 剧情演出音 / 小游戏音三层）。
**剧情侧无需改剧本加指令**，你们在编译器里按 `@scene` / `@ending` 规则挂即可。

### 4.3 CG 转 JPEG ✅ 确认可行（并已执行）
`JavaFX 不支持 WebP` 这点我方接受。**CG 的 `hires/` 已按 JPEG q92 + `subsampling=0`（4:4:4）转好**
（127 MB → 25 MB，PSNR 38~43 dB）。4:4:4 是为避免动漫线稿出现色度渗色，请勿再降采样。
游戏版（1536 宽）目前仍是 PNG；若要走全量入库，我同样可转 JPEG。

### 4.4 体积策略 ⚠️ 需项目主拍板
美术侧建议 **(c) + (b) 组合**：CG 压 JPEG 后入库 + hires 不入库（按需分发）。
理由：`cg/hires` 单目录 127 MB（转 JPEG 后 25 MB），而 hires 在游戏里只有「CG 鉴赏全屏」才用得到。

## 四、另两处需要你们知道的

1. **9 张被覆盖的素材是有意的** —— 按项目主指令「本生产工作区是唯一素材来源」，
   我同步了旧管线版本（旧版在 `assets/sprites/_old_pipeline_backup/` 留了备份）。
   覆盖的正是你们列的 9 张，**请保留新版**。
2. **§七 的口径已修正** —— 我方原文档把 `verify_handover.mjs` 说成查素材引用，那是错的。
   现补 `tools/check_asset_refs.py`：真正的 `path=` / `audio=` 引用完整性校验。

## 五、小游戏底图的排面约定（你们提的）

确认：底图的「棋盘暗区」是**中央那条深色石板路 / 空地**，所以**盘面一律居中摆放**，不贴角。
已写入 `小游戏底图.md`。若某个游戏需要贴角（例如 HUD 占位不同），告诉我单独出一版。
